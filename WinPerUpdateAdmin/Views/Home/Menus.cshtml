@foreach (var item in Model)
{
    if (Request.Path.Equals(item.Link))
    {
        <li class="active">
            <a class="navbar-brand" href="@item.Link">
                <span class="fa @item.Icon"></span>
                @item.Descripcion
            </a>
        </li>
    }
    else
    {
        <li>
            <a class="navbar-brand" href="@item.Link">
                <span class="fa @item.Icon"></span>
                @item.Descripcion
            </a>
        </li>
    }

}

